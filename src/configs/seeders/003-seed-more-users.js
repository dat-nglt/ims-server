"use strict";

/**
 * Seeder 003: Merged into 000-seed-users.js
 *
 * This seeder was merged into `000-seed-users.js`. Keeping a no-op to avoid duplicate inserts.
 */

export async function up(queryInterface, Sequelize) {
  // no-op (merged into 000-seed-users.js)
}

export async function down(queryInterface, Sequelize) {
  // no-op
}
